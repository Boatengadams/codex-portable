# Per-user logins and Codex state — never ship these
accounts/*
!accounts/.gitkeep

# Downloaded Codex binaries (fetched by setup.sh / setup.bat)
bin/**/*.tar.gz
*.tar.gz

# Local HyperFrames/brag production output
brag-output/

# OS junk
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
Desktop.ini
$RECYCLE.BIN/
*.swp
*.swo
*~
